#!/usr/bin/env bash

CD=$(cd "$(dirname "$0")" || exit && pwd)
cd "$CD" || exit
echo "Current Directory: $CD"

########################################################################################################################
#                                                       heu                                                            #
########################################################################################################################
"${CD}"/build_heu.sh

